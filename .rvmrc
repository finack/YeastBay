#!/usr/bin/env bash

ruby_string="ruby-1.9.2"
gemset_name="yeastbay"

rvm --create  "${ruby_string}@${gemset_name}"
if ! command -v bundle ; then
   echo "rvm: installing bundler"
   gem install bundler
fi
